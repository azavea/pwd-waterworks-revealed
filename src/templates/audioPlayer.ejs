<% if (zone.audio) { %>
    <span class="audio-control playing">Pause Audio</span>
    <audio autoplay>
        <% _.each(zone.audio.files, function(file) { %>
            <source src="<%= mediaPath %><%= file %>" type="" />
        <% }); %>
    </audio>
<% } %>
